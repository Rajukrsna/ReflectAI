<!DOCTYPE html>
<html lang="en">
<head>
    <title>Results</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h2>Analysis for: <%= healthProblem %></h2>
        <h4>Nutrient Deficiencies:</h4>
        <ul>
            <% deficiencies.forEach(deficiency => { %>
                <li><a href="/nutrient/<%= deficiency.trim() %>"><%= deficiency.trim() %></a></li>
            <% }); %>
        </ul>
        <form action="/yoga" method="POST">
            <input type="hidden" name="healthProblem" value="<%= healthProblem %>">
            <button type="submit" class="btn btn-secondary mt-3">Get Yoga Recommendations</button>
        </form>
        <a href="/" class="btn btn-secondary mt-3">Back</a>
    </div>
</body>
</html>
